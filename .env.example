# Environment Variables for HireMe Application
# Copy this file to .env and fill in your actual values

# OpenAI API Key for AI features (required for mock interviews, resume parsing)
OPENAI_API_KEY=your_openai_api_key_here

# Hugging Face API Key for additional AI features (optional)
HF_API_KEY=your_huggingface_api_key_here

# Other environment variables are configured in docker-compose.yml
# No additional setup required for Docker deployment
